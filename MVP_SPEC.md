# PromptHalo MVP 规格

## 产品定义

PromptHalo 不是 Prompt 数据库，也不是另一个通用启动器。

它是一个只有五个弹药位的 Mac 原生 Prompt 喊话轮盘：从任何输入框到插入完整 Prompt，不超过一次长按加一个数字。

## 核心动作

```text
当前输入框
  → 按住呼出键（默认长按 Tab，可自定义）
  → 光标旁出现 5 格轮盘
  → 按 1–5 或移动方向
  → 松开
  → Prompt 进入原输入框
```

## 交互约束

- 五个固定快捷位，不做多层菜单。
- 呼出键可以直接录制、持久化，并可恢复默认值；支持单独长按 Tab、左/右 Option 或普通组合键。
- 短按 Tab 必须保留 macOS 原有行为，只有持续约 0.22 秒才能呼出轮盘。
- 界面支持中文和英文；首次启动跟随 Mac 系统语言，也允许手动选择。
- 首次安装按界面语言写入对应语言的五条默认 Prompt；之后切换界面语言不得覆盖用户已有 Prompt。
- 轮盘只显示编号和名称，不展开 Prompt 全文。
- 指针停在圆心并松开，什么都不做。
- 按 `Esc` 取消。
- 没有辅助功能权限时，只复制并明确提示。
- Prompt 空位不能假装执行成功。
- 先关闭浮层，再向原应用发送粘贴。
- 原剪贴板在粘贴后恢复；用户期间修改过剪贴板则不恢复。

## Mac 原生边界

- SwiftUI 管理页
- AppKit 非激活 `NSPanel` 轮盘
- SF Pro、SF Symbols、系统材质与强调色
- 菜单栏常驻，默认不显示 Dock 图标
- 本地 JSON，不要求注册或登录

## 验收

唯一关键验收先在“备忘录”完成：

1. 新建一条 500 字、多行、含 Markdown 的中文 Prompt。
2. 分配到位置 3。
3. 关闭管理页。
4. 在备忘录输入框长按 `Tab`，按 `3`，松开。
5. 全文原样插入，换行不丢，原剪贴板不丢。
6. 整个调用到插入过程低于 1 秒。

通过后，再测试 Chrome、ChatGPT、飞书和微信。
